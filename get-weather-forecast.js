const getWeather = (year, month, day) => {
  // TODO Make some kind of made up weather data from param values
  return {
    degrees: 10,
    descriptive: 'Cloudy and raining'
  }
}

module.exports = getWeather;
